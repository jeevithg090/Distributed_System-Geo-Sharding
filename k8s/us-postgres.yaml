apiVersion: apps/v1
kind: Deployment

metadata:
  name: us-postgres

spec:
  replicas: 1

  selector:
    matchLabels:
      app: us-postgres

  template:

    metadata:
      labels:
        app: us-postgres

    spec:

      containers:

      - name: postgres

        image: postgres:16

        ports:
        - containerPort: 5432

        env:
        - name: POSTGRES_USER
          value: admin

        - name: POSTGRES_PASSWORD
          value: password

        - name: POSTGRES_DB
          value: sharddb
---
apiVersion: v1
kind: Service

metadata:
  name: us-postgres-service

spec:

  selector:
    app: us-postgres

  ports:
    - port: 5432
      targetPort: 5432
